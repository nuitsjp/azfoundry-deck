# 作業計画と未決事項

本書は進捗状況、作業順序、未決・未実装・未検証事項を管理する正本です。確定した仕様や検証結果は [プロジェクト定義](docs/project.md) を参照します。

## 1. 現在の到達点

テンプレート配布版2に基づく初期文書を整備し、仮実装から要件・制約および初期機能（F1）のシナリオ案を整理しました。

F1 モック用の最小構成を選定し、互換性確認用ひな形で依存取得、バインディング生成、型検査、Windows ビルドを確認しました。結果と証跡は [検証結果](docs/project.md#verification) に記録しています。

逐次ヒアリングにより、技術構成の採用と [F1 の主要要件](docs/project.md#f1-agreement) を合意しました。F1 全シナリオのモック作成、動作するモックによる仕様合意、実処理接続、動作検証は未着手です。

## 2. 次に進める作業

- [x] テンプレートの適用および初期文書・未決事項の整理
- [x] F1 モックの最小構成選定と、ツール・依存ライブラリの互換性確認（U2 のモック用構成）
- [x] F1 の主要要件のヒアリングと、モック確認用シナリオ案の更新
- [ ] F1 モックの作成と、起動・状態再現手順の確立
- [ ] 動作する F1 モックと要件案の提示による仕様合意（U1）
- [ ] F1 実処理への接続環境構築と、実接続・エラーハンドリングの検証（U2、U3）
- [ ] F1 検証完了後、F2〜F4 の順次具体化と、モック合意・実装・検証の反復（F3 着手前に U4 を解消）

F1 はアカウント横断のデプロイ一覧と絞り込みを扱います。各行の所属アカウント情報を後続機能で利用しますが、後続機能の詳細設計やモックは先行して作成しません。

## 3. 未決・未検証事項

解消時は状態と確定先への参照を更新します。

| ID | 状態 | 論点・前提と根拠 | 影響する機能・依存範囲 | 解消条件 | 確定先・確認結果 |
| --- | --- | --- | --- | --- | --- |
| U1 | 一部合意済 | F1 の主要要件と技術構成はヒアリングで合意済み。絞り込み操作・列配置・状態表示を含むモックでの動作合意、および残る制約の確認は未完了。 | F1 の実処理接続、後続機能の範囲 | 合意要件を反映した F1 モックと残る制約案を提示し、仕様合意を記録する。後続機能は各着手時に合意する。 | [ヒアリング結果](docs/project.md#f1-agreement)、[F1](docs/project.md#f1) |
| U2 | 一部確認済 | モック用構成の依存取得・コード生成・型検査・Windows ビルドは合格し、依存を固定済み。実接続用 Azure SDK の依存構成・ビルド、および Windows でのネイティブ起動は未検証。 | モックの開発環境、バインディング生成、型検査、Windows ビルド | 採用構成の依存解決・コード生成・型検査・ビルドを確認し、生成した lockfile 等を保存する。実接続用構成とネイティブ起動の残りは実処理接続時に確認する。 | [構成と選定理由](docs/project.md#design)、[U2 の検証結果](docs/project.md#verification) |
| U3 | 未検証 | 複数テナントでの認証と対象探索の網羅性、設定 TPM/RPM の取得、権限、および全件取得の所要時間が未検証。 | F1 の実接続検証と性能に基づく初期表示範囲の判断（後続機能は必要権限を都度確認） | アクセス権のある対象を漏れなく列挙できるか確認し、設定値を照合する。認証・権限・通信障害と部分失敗を検証する。全件取得時間を測定し、既定の表示範囲を確定する。 | [F1](docs/project.md#f1)、[レート制限の外部仕様](docs/project.md#rate-limit-source)、[検証結果](docs/project.md#verification) |
| U4 | 未決 | 仮実装の記録では、既存名確認と作成要求は他アプリとの間で一体の操作として保証されず、タイムアウトも Azure 側の変更取消を保証しない。 | F3 の作成・更新・削除と実環境検証 | F3 着手時に外部仕様を確認し、競合時・結果不明時の振る舞いと表示、検証用リソース・費用条件を合意する。 | [参照資料 R1・R2](docs/project.md#references)、[後続機能](docs/project.md#later-features) |

## 4. 機能の進捗

機能 ID は仕様と統一し、合意・実装・検証の進捗を個別に管理します。完了判定は [標準の完了基準](docs/standards/design-and-documentation.md#completion) に従います。

| 機能 ID・仕様への参照 | 仕様合意 | 実装 | 検証 | 残る課題 ID |
| --- | --- | --- | --- | --- |
| [F1: アカウント横断のデプロイ一覧と絞り込み](docs/project.md#f1) | 主要要件は合意済み、モック動作は未合意 | 未着手（U2 のひな形あり） | 動作未検証（モック用構成のビルドは合格） | U1、U2、U3 |
| [F2: モデル・バージョン・SKU 候補の参照](docs/project.md#f2) | 未合意（範囲案のみ） | 未着手 | 未検証 | U1、U2、U3 |
| [F3: デプロイの作成・変更・削除](docs/project.md#f3) | 未合意（範囲案のみ） | 未着手 | 未検証 | U1、U2、U3、U4 |
| [F4: クォータの参照](docs/project.md#f4) | 未合意（範囲案のみ） | 未着手 | 未検証 | U1、U2、U3 |
